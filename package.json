{
  "name": "elepay-apply-autofill-agent",
  "version": "1.0.0",
  "private": true,
  "description": "一个**纯浏览器内**的 agent：在加盟店申请表单页面注入右下角浮窗，点「开始」后，本地推理服务的模型通过 DOM 工具逐步识别并填写表单——读真实下拉选项、按校验纠错、一路点到最终确认页停下（绝不提交）。仅 jsdom 测试依赖 npm；扩展本身零构建。",
  "scripts": {
    "test": "node scripts/test-snapshot.mjs && node scripts/test-choose.mjs"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}